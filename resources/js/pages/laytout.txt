import AppLayout from '@/layouts/app-layout';
import { Head } from '@inertiajs/react';

type AuthUser = { name: string; email: string };

interface LayoutItem {
    id?: number | string;
    // extend fields as needed
}

interface LayoutIndexProps {
    user: AuthUser;
    layout?: LayoutItem[];
}

export default function LayoutIndex({ user, layout = [] }: LayoutIndexProps) {
    return (
        <AppLayout user={user}>
            <div>

            </div>
        </AppLayout>
    );
} 